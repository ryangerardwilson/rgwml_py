def show_docs():
    """Prints out all methods and classes in rgwml package with their signatures."""
    items = [
        "add_one(number)",
        "add_two(number)",
    ]
    for item in items:
        print(item)
