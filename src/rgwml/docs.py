def show_docs():
    """Prints out all methods in rgwml package with their signatures."""
    methods = [
        "add_one(number)",
        "add_two(number)",
    ]
    for method in methods:
        print(method)
